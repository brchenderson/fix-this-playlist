import React from 'react';
import {Link} from 'react-router-dom';

const SearchPage = () => (
  <div>
    <h1>Eventually you can search here.</h1>
    <Link to="/">For now go back home.</Link>
  </div>
);

export default SearchPage;
